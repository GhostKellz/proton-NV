From: GhostKellz <chris@ghostkellz.sh>
Subject: [PATCH 1/5] meson: Add nvvk dependency for NVIDIA Reflex support

Add optional nvvk library dependency to enable VK_NV_low_latency2
integration for NVIDIA Reflex in vkd3d-proton (DX12).

---
 meson.build | 14 ++++++++++++++
 1 file changed, 14 insertions(+)

diff --git a/meson.build b/meson.build
index 1234567..abcdefg 100644
--- a/meson.build
+++ b/meson.build
@@ -150,6 +150,20 @@ if enable_renderdoc
   add_project_arguments('-DVKD3D_RENDERDOC', language : 'c')
 endif

+# Optional nvvk library for NVIDIA Reflex support
+nvvk_dep = dependency('nvvk', required : false)
+vkd3d_nvvk_enabled = nvvk_dep.found()
+
+if vkd3d_nvvk_enabled
+  message('nvvk found - NVIDIA Reflex support enabled')
+  add_project_arguments('-DVKD3D_NVVK_ENABLED=1', language : 'c')
+  vkd3d_extra_deps += nvvk_dep
+else
+  message('nvvk not found - NVIDIA Reflex support disabled')
+  add_project_arguments('-DVKD3D_NVVK_ENABLED=0', language : 'c')
+endif
+vkd3d_nvvk_enabled_int = vkd3d_nvvk_enabled ? 1 : 0
+
 subdir('include')
 subdir('libs')

